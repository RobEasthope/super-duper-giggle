import styled from 'styled-components';

export const Styles = styled.div`
  .wrapper {
    min-height: 100vh;
    max-width: 40em;
    margin: 0 auto;
  }

  .permissions-msg {
    text-align: center;
  }

  .weather-icon {
    display: block;
    margin: 0 auto;
  }
`;
